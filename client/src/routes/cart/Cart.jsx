import React from "react";
import styled from "./cart.module.css";

const Cart = (props) => {
    return (
        <>
            <div className={styled.page_name}>CART</div>
        </>
    );
};

export default Cart;
